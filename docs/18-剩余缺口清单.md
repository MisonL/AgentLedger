# AgentLedger 剩余缺口清单

- 文档版本：v1.2
- 更新时间：2026-03-07
- 目标：只保留“本轮全量复核后仍然明确存在”的缺口，并把“待开发”和“待补强”分层收口成可执行 backlog。

## 1. 待补强项（已有基础实现，但未达到规划完全体）

### 1.1 企业治理深化

1. 告警编排中心的工单系统联动深化
   - 当前已有规则、模拟、执行日志、`ticket` 下游分发与跨服务 smoke
   - 仍缺工单状态回写、`case/incident` 实体同步与 ACK/Resolve 闭环映射

2. 告警编排中心的 SLA 升级执行链
   - 当前文档有目标描述
   - 但未见明确升级执行链、升级目标与回归证据

3. 数据主权的 KMS / 区域归档映射
   - 当前已有 policy / region mappings / replication jobs
   - 仍缺区域级密钥管理映射与区域归档策略落地

4. MCP 更复杂审批编排
   - 当前已有 policy / approvals / evaluate / invocations 首版闭环
   - 仍缺更复杂的多级审批、动态条件与企业编排能力

5. quality 高级版预测与建议
   - 当前已有评估写入、日报、相关性、项目趋势、评分卡
   - 仍缺预测、建议、自动治理联动等高级能力

6. replay “实验室”完全体
   - 当前已有 datasets / runs / diffs / artifacts / materialize 基础能力
   - 仍缺更完整的实验编排、基线管理扩展与实验室工作流

## 2. 待开发项（当前无明确实现入口）

### 2.1 企业治理 / 身份 / 合规

1. Agent 生命周期审计
   - 平台侧审计已完整
   - 但未见 Agent 安装/升级/卸载审计入口

2. SCIM / SAML / 强制 MFA / 风险登录
   - 当前已有 provider discovery、OAuth/OIDC、外部断言登录
   - 更深企业 IAM 集成仍未落地

3. DLP / Legal Hold
   - 当前有 evidence bundle 与审计导出
   - 但未见 DLP 与 Legal Hold 代码入口

### 2.2 企业 C/S 深化

1. Store-and-Forward 本地持久队列
2. Policy Agent / 远程配置下发
3. Agent Updater / 自动升级
4. 静默安装与批量部署资产

这些项在 `docs/10` 中属于企业 C/S 完整体，目前未见明确实现入口。

### 2.3 部署形态长期蓝图

1. 多环境 / 多集群 / SaaS 多租户部署形态
   - 当前已具备 monorepo 与服务拆分
   - 但未见完整多环境、多集群、SaaS 控制面部署资产

## 3. 验证与文档结论

1. `docs/00`、`docs/01`、`docs/11`、`docs/15`、`docs/16`、`docs/17`、`docs/18` 本轮已完成同步
   - 这些基础事实文档不再属于“缺文档”

2. 本轮发布说明已补齐：
   - `docs/releases/2026-03-07-rule-hub-dual-approval-diff-and-agent-mtls-e2e.md`
   - 用于长期保留“Rule Hub 双人审批 / 版本 diff、Agent gRPC mTLS E2E、规划复核与 backlog 分层”结论

3. 某些长期蓝图项只有文档，没有代码，也就不存在验证入口
   - 这类项不应误记成“验证失败”
   - 应继续维持为 `仅规划/长期蓝图`

4. 如后续继续推进企业能力深化，应继续按能力批次补充新的 `docs/releases/*`
   - 避免后续交接继续依赖历史聊天记录

## 4. 建议实施顺序

1. 第一批：告警编排中心工单联动、SLA 升级链、数据主权 KMS / 区域归档映射
   - 理由：都属于既有治理主线的直接补强，业务收益高，复用现有数据结构最多

2. 第二批：MCP 更复杂审批编排、quality 高级版预测与建议、replay 实验室完全体
   - 理由：已有基础能力与工作台，适合在现有 API / 控制台上逐步迭代

3. 第三批：Agent 生命周期审计、SCIM / SAML / MFA / 风险登录、DLP / Legal Hold
   - 理由：牵涉身份体系和合规域，跨模块改动较大，适合单独立项

4. 第四批：Store-and-Forward、本地持久队列、远程配置、Updater、静默安装、多环境多集群部署
   - 理由：属于企业 C/S 与平台形态长期蓝图，设计与交付成本最高，应避免与当前治理主线混做

## 4.1 分批开发计划矩阵

| 批次 | 能力项 | 目标交付 | 主入口建议 | 验证要求 |
| --- | --- | --- | --- | --- |
| 第一批 | 告警编排中心工单联动深化 | 在现有 `ticket` downstream 基础上补状态回写、`case/incident` connector、失败审计与治理页闭环 | `apps/control-plane/src/routes/alerts.ts`、`services/governance`、`services/integration`、governance 工作台 | `bun run test`、保留 `test:e2e-governance-integration-downstream`，并新增工单回写 / `incident` 定向回归 |
| 第一批 | 告警编排中心 SLA 升级执行链 | 为 alert execution 增加升级窗口、升级目标、升级原因和执行日志 | `services/governance`、`apps/control-plane/src/routes/alerts.ts`、`apps/web-console/src/App.tsx` | `go test ./services/governance`、新增 SLA 升级定向测试与工作台回归 |
| 第一批 | 数据主权 KMS / 区域归档映射 | 在 residency policy / region mapping 之外补 KMS key mapping、archive region policy、审批与审计 | `apps/control-plane/src/routes/residency.ts`、`routes/api-v2.ts`、repository、governance 工作台 | `bun run test`、新增 API v2 residency 回归与控制台回归 |
| 第二批 | MCP 更复杂审批编排 | 补多级审批、动态条件、策略命中后的审批编排状态机 | `apps/control-plane/src/routes/mcp.ts`、repository、contracts、web-console governance | `bun run test`、新增 MCP 审批编排定向用例 |
| 第二批 | quality 高级版预测与建议 | 在基础质量看板外增加趋势预测、建议项、治理联动入口 | `apps/control-plane/src/routes/quality.ts`、`routes/api-v2.ts`、analytics、web-console governance | `bun run test`、`go test ./services/analytics`、新增控制台查询回归 |
| 第二批 | replay 实验室完全体 | 补实验编排、基线管理扩展、批量对比、实验工作流视图 | `apps/control-plane/src/routes/replay.ts`、`routes/api-v2.ts`、`apps/web-console/src/App.tsx` | `bun run test`、`bun run sdk:check`、新增 replay 真实工作流回归 |
| 第三批 | Agent 生命周期审计 | 为 install / upgrade / uninstall / doctor 结果补平台可检索审计入口 | `clients/agent`、`apps/control-plane/src/routes/audits.ts`、identity / agent binding 相关路由 | `go test ./clients/agent`、`bun run test`、新增审计查询用例 |
| 第三批 | SCIM / SAML / MFA / 风险登录 | 在现有 OAuth/OIDC 基础上补企业身份目录同步、外部 IdP、登录风险控制 | `apps/control-plane/src/routes/auth.ts`、`identity.ts`、contracts、web-console auth | `bun run test`、新增 auth / identity 回归矩阵 |
| 第三批 | DLP / Legal Hold | 补合规留存策略、保全锁定、导出与审计联动 | `apps/control-plane/src/routes/audits.ts`、security / evidence bundle、governance 工作台 | `bun run test`、新增 evidence / audit / hold 相关回归 |
| 第四批 | Store-and-Forward 本地持久队列 | 为 agent `run` 增加磁盘队列、重试、恢复和积压观测 | `clients/agent`、`services/ingestion-gateway` | `go test ./clients/agent ./services/ingestion-gateway`、FR-505 衍生 smoke |
| 第四批 | Policy Agent / 远程配置下发 | 平台可向 agent 下发采集/推送/治理配置并支持版本化 | `clients/agent`、`apps/control-plane`、可能新增配置服务/路由 | `bun run test`、`go test ./clients/agent`、新增配置下发 E2E |
| 第四批 | Agent Updater / 自动升级 | 补版本清单、签名校验、灰度升级与回滚 | `clients/agent`、release / artifact 脚本、文档 | `go test ./clients/agent`、新增升级 smoke |
| 第四批 | 静默安装与批量部署资产 | 为三平台补企业安装参数、静默安装模板与资产分发 | `scripts/`、`README`、`docs/05`、agent 打包链路 | `bun run smoke:fr505`、新增静默安装文档验收脚本 |
| 第四批 | 多环境 / 多集群 / SaaS 多租户部署 | 形成部署拓扑、配置分层、控制面边界与运维资产 | 部署目录、`docs/10`、CI / release / infra 资产 | 以部署文档、样例清单和 smoke 为主，非单仓内短期开发项 |

## 5. 当前不应再重复开发的内容

以下内容本轮已确认“已实现且已验证”，不应再被误判成待开发：

1. 数据主权首版（policy / mappings / replications）
2. Rule Hub 首版闭环
3. Rule Hub 双人审批、版本 diff、审批审计闭环
4. MCP 工具治理首版闭环
5. 告警编排中心真实分发闭环
6. 合规取证包
7. OpenAPI / Webhook / API Key / 8 语言 SDK
8. quality v2 基础版
9. replay v2 基础版
10. integration callback / routing / downstream smoke
11. Agent 多架构构建、FR-505、CI / Release 门禁链
12. Agent Push gRPC mTLS 端到端验证入口（本地 Go E2E 已覆盖握手、请求、响应）
