# AgentLedger 回归验证执行记录

- 文档版本：v1.6
- 更新时间：2026-03-11
- 目标：记录本轮“全量规划功能复核”实际执行过的验证命令、结果与覆盖范围。

## 0. 增量回归记录

### 0.1 2026-03-11 增量回归（quality modelVersion/timeseries + agent 队列损坏隔离）

说明：本节记录的是在 Codex worktree 上对 2026-03-11 的两项增量（quality `modelVersion`/timeseries、agent 队列损坏隔离）执行的最小定向回归。

- 执行仓库：`/Volumes/Work/code/AgentLedger`
- 基线提交：`3f6d0ba`
- `bun run proto:gen`：通过（生成 `packages/gen/go`/`packages/gen/ts`，供 Go 包编译）
- `go test ./clients/agent -run TestRunCommand_WithQueueSkipsCorruptEntriesAndQuarantinesFiles -count=1`：通过
- `bun run --cwd apps/control-plane test test/api.test.ts -t "api-v2 quality 路由：forecast 与 advice"`：通过
- `bun run --cwd apps/control-plane test test/api.test.ts -t "api-v2 quality 路由：forecast 支持 quality-timeseries-v1"`：通过
- `bun run test`：通过（Go 全量 + control-plane 全量 + web-console 全量）

### 0.2 2026-03-10 增量回归（本地未提交改动）

说明：本节记录的是在 `wave7-rulehub-replay` 分支上的增量回归结果（现已快进合入 `main`），用于证明该批改动可复现通过；CI 证据见第 1 节远端基线。

- `bun run --cwd apps/web-console test -- --test-name-pattern "治理页开放平台工作台支持 OpenAPI/API Key/Webhook/Quality/Replay 基础联调"`：通过
- `bun run test`：通过（Go 全量 + control-plane 全量 + web-console 全量）
- `go test ./services/archiver`：通过（覆盖 `ARCHIVE_LOCAL_COMPRESSION` 的新增用例）
- `RULEHUB_E2E_DATABASE_URL=... AGENTLEDGER_E2E=1 bun run test:e2e-rulehub-approvals-db`：通过（真实 Postgres；Rule Hub 审批 upsert created/updated 审计一致性）

## 1. 执行环境

1. 本地仓库：`/Volumes/Work/code/AgentLedger`
2. 当前分支：`main`
3. 远端基线证据：
   - GitHub Actions Run `22788548732`
   - 结论：`success`
   - 覆盖：三平台 baseline + agent 多架构构建
4. 真实治理链 E2E：
   - 使用 `postgres:16-alpine` 临时容器
   - 本地固定端口：`55433`

## 2. 本轮已执行命令

| 命令 | 结果 | 主要覆盖范围 |
| --- | --- | --- |
| `bun run lint` | 通过 | TypeScript lint / `tsc --noEmit` 总体门禁 |
| `bun run check:ts` | 通过 | control-plane / web-console / SDK TypeScript 检查 |
| `bun run check:support-matrix` | 通过 | docs/09、puller connectors、agent collect、parser 入口一致性 |
| `bun run sdk:check` | 通过 | `clients/sdk`、`dist/sdk`、`SHA256SUMS` 只读校验 |
| `bun run test:scripts` | 通过 | support matrix / evidence bundle / source-region / sdk-common 脚本测试 |
| `bun run sdk:verify` | 通过 | SDK 目录完整性与 operation 覆盖 |
| `bun run sdk:test` | 通过 | SDK 生成产物测试 |
| `bun run test` | 通过 | Go 全量、control-plane 全量、web-console 全量 |
| `bun run build` | 通过 | `go build ./...` + web-console build |
| `bun run test:coverage` | 通过 | Go 覆盖率 + control-plane 覆盖率门禁 |
| `bun run check:callback-stream-binding` | 通过 | governance alerts / integration callback stream 绑定一致性 |
| `bun run test:callback-chain-targeted` | 通过 | integration callback + control-plane callback action/幂等链 |
| `bun run test:e2e-integration-routing` | 通过 | integration 真消费分发 E2E（alert/weekly/callback/channels） |
| `bun run test:e2e-governance-callback-chain` | 通过 | governance -> integration -> control-plane 回调闭环冒烟 |
| `bun run smoke:fr505` | 通过 | FR-505 三平台最小安装运行冒烟 |
| `bun run build:agent-cross && bun run verify:agent-cross` | 通过 | Agent 多架构构建与产物校验 |
| `bun run package:agent-distribution` | 通过 | 生成 `dist/agent-distribution/<os>/<arch>/agent-*.tar.gz + release-manifest.json + package/*` 最小企业分发包 |
| `bun run verify:agent-distribution` | 通过 | 组装后按本机平台执行 `version/status/update check/download/status` 与 checksum 验收 |
| `go test ./clients/agent -run TestSendIngestRequestGRPC_MTLSEndToEnd -count=1` | 通过 | Agent Push 的 gRPC mTLS 本地端到端验证 |
| `bun run --cwd apps/control-plane test test/integration-event-publisher.test.ts` | 通过 | control-plane 外部状态同步事件的 JetStream `PubAck` 发布确认定向回归 |
| `bun run --cwd apps/control-plane test test/api.test.ts -t "mcp 路由："` | 通过 | MCP 审批编排补充回归：拒绝 `approvalMode`/阶段数不一致策略，保留 `approvalStages.nodeId/label`，并继续覆盖静态审批、workflow 分支、timeWindow |
| `bun run --cwd apps/web-console test -- test/api.test.ts -t "mcp api helpers"` | 通过 | web-console MCP API helper 回归：策略 query/upsert、审批 create/approve、evaluate、invocations/list 契约校验 |
| `bun run --cwd apps/web-console test -- test/app.test.tsx -t "治理页 MCP"` | 通过 | 治理页 MCP 回归：双阶段、多阶段 workflow、`approvalStages JSON` 生效、节点改名后的入口/转移引用同步、`timeWindow` 评估 |
| `bunx tsc -p apps/control-plane/tsconfig.json --noEmit` | 通过 | control-plane `quality advice executions` / `replay experiments` 持久化类型检查 |
| `bunx tsc -p apps/web-console/tsconfig.json --noEmit` | 通过 | web-console `defaultActionType`、`trendDirection/projectedDelta/basisWindowCount`、`runStatusSummary/aggregateSummary` 类型检查 |
| `bun run --cwd apps/control-plane test test/repository.test.ts -t "quality advice execution 支持 upsert/list/get latest\|replay experiment 支持持久化 runIds 与状态更新"` | 通过 | repository 新增 `quality_advice_executions` 与 `replay_experiments/replay_experiment_runs` 持久化回归 |
| `bun run --cwd apps/control-plane test test/api.test.ts -t "api-v2 quality 路由：评估写入、指标查询、成本相关性与评分卡更新"` | 通过 | 自动治理命中后沉淀真实 advice execution、approvalRequest 关联、`mcp approval -> advice execution` 审批后继续执行/驳回收口，以及质量回归联动 |
| `bun run --cwd apps/control-plane test test/api.test.ts -t "api-v2 quality 路由：advice execute/list/cancel\|api-v2 replay 路由：experiments create/list/detail\|api-v2 replay 路由：experiment run/cancel/results/artifacts"` | 通过 | quality advice execution / replay experiment 持久化后主链行为回归，覆盖 experiment `compare/workflow` 查询 |
| `bun run --cwd apps/web-console test -- test/api.test.ts -t "open platform 支持 quality advice execution 与 replay experiment workflow"` | 通过 | web-console API helper 回归：quality advice execution、replay experiment 契约 |
| `bun run --cwd apps/web-console test -- test/app.test.tsx -t "治理页开放平台工作台支持 OpenAPI/API Key/Webhook/Quality/Replay 基础联调"` | 通过 | Governance 页 Open Platform 工作台回归：forecast/advice explain、advice 一键带入执行表单、advice executions、replay experiments compare/workflow UI 闭环 |
| `bun run --cwd apps/control-plane test test/api.test.ts -t "api-v2 replay 路由：experiment run/cancel/results/artifacts"` | 通过 | replay experiments 新增批量 compare 入口后的 control-plane 定向回归 |
| `bunx vitest run apps/web-console/test/api.test.ts -t "open platform 支持 quality advice execution 与 replay experiment workflow" --pool=threads` | 通过 | 绕过 vitest forks worker 启动超时后的 web-console API helper 定向回归，覆盖 replay experiments 批量 compare 契约 |
| `cd apps/web-console && bunx vitest run test/app.test.tsx -t "治理页开放平台工作台支持 OpenAPI/API Key/Webhook/Quality/Replay 基础联调" --pool=vmThreads --maxWorkers=1` | 通过 | Governance 页 Open Platform 工作台新增批量 compare UI 回归；本地需规避 worker 启动超时 |
| `bun run --cwd apps/control-plane test test/api.test.ts` | 通过 | control-plane API 全量回归，确认 `quality/replay` 从进程内 Map 下沉到 repository 后未破坏既有 v1/v2 路由 |
| `bun run --cwd apps/web-console test -- test/api.test.ts test/app.test.tsx` | 通过 | web-console API + 治理页全量回归，覆盖开放平台工作台与其它治理能力联调 |
| `bun run --cwd apps/control-plane test test/api.test.ts -t "TokenPulse runtime events 路由"` | 通过 | TokenPulse 运行时摘要入站签名校验、幂等、租户隔离与审计回归 |
| `bun run --cwd apps/web-console test -- test/app.test.tsx -t "TokenPulse 运行时摘要|按 traceId 查询 TokenPulse"` | 通过 | Governance 页 TokenPulse Runtime Events 联查面板回归 |
| `go test ./services/integration -run 'TestHandleAlertExternalStatusSyncMessage|TestEnsureDLQStream' -count=1` | 通过 | integration 外部状态同步、callback 失败补偿与 DLQ stream 自确保定向回归 |
| `AGENTLEDGER_E2E=1 GOV_E2E_DATABASE_URL=... bun run test:e2e-governance-routing` | 通过 | governance 真实 PG + 嵌入式 NATS E2E |
| `AGENTLEDGER_E2E=1 GOV_E2E_DATABASE_URL=... bun run test:e2e-governance-integration-downstream` | 通过 | 2026-03-10 已在真实 PG（`postgres:16-alpine`；`127.0.0.1:55433`）实跑通过，覆盖 governance -> integration -> downstream 跨服务 smoke，并覆盖 `control-plane alert status PATCH -> integration.alert.external_status_sync -> downstream -> sync_external_link_result callback` 回写闭环 |

## 3. 关键结果摘要

### 3.1 主门禁

1. `lint / check:ts / test / build` 全部通过。
2. `check:support-matrix / sdk:check / sdk:verify / sdk:test / test:scripts` 全部通过。
3. `test:coverage` 通过，关键阈值结果为：
   - `ingestion-gateway`：`77.44%`
   - `puller`：`74.68%`
   - `integration`：`76.84%`
   - `control-plane All files`：`81.79%`

### 3.2 治理与集成链路

1. callback stream 绑定检查通过。
2. callback 定向回归通过。
3. `integration routing E2E` 通过，覆盖：
   - `webhook`
   - `wecom`
   - `dingtalk`
   - `feishu`
   - `email`
   - `email_webhook`
   - `incident`
   - `ticket`
   - callback NATS forward
4. `governance callback chain` 冒烟通过。
5. 真实 PostgreSQL 下：
   - `test:e2e-governance-routing` 通过
   - `test:e2e-governance-integration-downstream` 通过
6. `TokenPulse -> AgentLedger` 第二阶段最小联调的 AgentLedger 侧基础能力已本地验证：
   - 签名 webhook 入站
   - `idempotency-key` 去重
   - `traceId` 查询与治理页只读联查
7. 告警外部状态同步的可靠性补强已本地验证：
   - control-plane 改为 JetStream `PubAck` 发布确认
   - integration 启动时自动确保 `INTEGRATION_DISPATCH_DLQ`
   - `sync_external_link_result` callback 失败时可转入 DLQ

### 3.3 三平台与交付

1. `smoke:fr505` 通过。
2. Agent 多架构构建通过，产物覆盖：
   - `linux/amd64`
   - `linux/arm64`
   - `darwin/amd64`
   - `darwin/arm64`
   - `windows/amd64`
   - `windows/arm64`
3. Agent 分发包组装与本机验收通过，当前已验证：
   - `dist/agent-distribution/<os>/<arch>/agent-*.tar.gz`
   - `release-manifest.json`
   - `package/.env.example`
   - `package/AGENT_RELEASE_SIGNING_PUBLIC_KEY.pem.example`
   - bundle/package 两层 `SHA256SUMS.txt`
4. Agent gRPC mTLS 本地端到端验证通过。

## 4. 未执行项

本轮规划中的主要回归命令均已执行，没有留空项。

## 5. 与功能矩阵的关系

1. `docs/16-全量规划功能核对矩阵.md` 负责回答“每个功能项现在是什么状态”。
2. 本文档负责回答“这些状态有没有被实际验证过”。
3. 需要后续继续开发的项，统一收口到 `docs/18-剩余缺口清单.md`。
