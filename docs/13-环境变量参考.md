# AgentLedger 环境变量参考

- 文档版本：v2.7
- 更新时间：2026-03-04
- 适用范围：`services/ingestion-gateway`、`services/integration`、`services/puller`、`apps/control-plane`、`clients/agent`

## 1. 使用约定

1. 所有变量名采用全大写下划线。
2. 未特别说明时，默认值仅用于本地开发，不代表生产建议值。
3. 涉及回调鉴权的密钥变量必须在 integration 与 control-plane 两端保持一致。

## 2. ingestion-gateway 关键变量

| 变量名                      | 默认值                  | 必填条件                        | 说明                                          |
| --------------------------- | ----------------------- | ------------------------------- | --------------------------------------------- |
| `NATS_URL`                  | `nats://127.0.0.1:4222` | 否                              | NATS 连接地址。                               |
| `HTTP_ADDR`                 | `:8081`                 | 否                              | HTTP 监听地址。                               |
| `GRPC_ADDR`                 | `:9091`                 | 否                              | gRPC 监听地址。                               |
| `GRPC_TLS_ENABLED`          | `true`                  | 否                              | 是否启用 gRPC TLS。                           |
| `GRPC_TLS_CERT_FILE`        | 空                      | `GRPC_TLS_ENABLED=true` 时必填  | gRPC 证书（PEM）。                            |
| `GRPC_TLS_KEY_FILE`         | 空                      | `GRPC_TLS_ENABLED=true` 时必填  | gRPC 私钥（PEM）。                            |
| `GRPC_MTLS_ENABLED`         | `false`                 | 否                              | 是否启用 mTLS。                               |
| `GRPC_TLS_CLIENT_CA_FILE`   | 空                      | `GRPC_MTLS_ENABLED=true` 时必填 | 客户端 CA 证书。                              |
| `OIDC_ISSUER`               | 空                      | 启动必填                        | OIDC issuer 校验值。                          |
| `OIDC_AUDIENCE`             | 空                      | 启动必填                        | OIDC audience 校验值。                        |
| `OIDC_JWKS_URI`             | 空                      | 启动必填                        | JWKS 地址。                                   |
| `OIDC_CLIENT_ID`            | 空                      | 否（见说明）                    | 设备码代理默认 `client_id`。                  |
| `OIDC_DEVICE_AUTH_ENDPOINT` | 空                      | 设备码 start 接口需要           | 未配置时 `/v1/oidc/device/start` 返回 `503`。 |
| `OIDC_TOKEN_ENDPOINT`       | 空                      | 设备码 poll 接口需要            | 未配置时 `/v1/oidc/device/poll` 返回 `503`。  |

## 3. integration 关键变量

### 3.1 告警分发

| 变量名                             | 默认值                                 | 必填条件               | 说明                                                                                                    |
| ---------------------------------- | -------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------- |
| `INTEGRATION_STREAM`               | `GOVERNANCE_ALERTS`                    | 否                     | 告警 stream 名称。                                                                                      |
| `INTEGRATION_SUBJECT`              | `governance.alerts`                    | 否                     | 告警订阅主题。                                                                                          |
| `INTEGRATION_DURABLE`              | `INTEGRATION_ALERTS_DISPATCHER`        | 否                     | 告警 durable consumer。                                                                                 |
| `INTEGRATION_WEEKLY_STREAM`        | `GOVERNANCE_REPORTS_WEEKLY`            | 否                     | 周报 stream。                                                                                           |
| `INTEGRATION_WEEKLY_SUBJECT`       | `governance.reports.weekly`            | 否                     | 周报主题。                                                                                              |
| `INTEGRATION_WEEKLY_DURABLE`       | `INTEGRATION_WEEKLY_REPORT_DISPATCHER` | 否                     | 周报 durable consumer。                                                                                 |
| `INTEGRATION_DLQ_SUBJECT`          | `integration.dispatch`                 | 否                     | 分发失败 DLQ 主题。                                                                                     |
| `INTEGRATION_CHANNELS`             | `webhook`                              | 否                     | 启用通道列表：`webhook,wecom,dingtalk,feishu`；`webhook` 原样转发事件，机器人通道发送 `text` 模板消息。 |
| `INTEGRATION_WEBHOOK_URL`          | 空                                     | 启用 `webhook` 时必填  | Webhook 地址（`http/https`）。                                                                          |
| `INTEGRATION_WECOM_WEBHOOK_URL`    | 空                                     | 启用 `wecom` 时必填    | 企业微信机器人地址。                                                                                    |
| `INTEGRATION_DINGTALK_WEBHOOK_URL` | 空                                     | 启用 `dingtalk` 时必填 | 钉钉机器人地址。                                                                                        |
| `INTEGRATION_FEISHU_WEBHOOK_URL`   | 空                                     | 启用 `feishu` 时必填   | 飞书机器人地址。                                                                                        |
| `INTEGRATION_ROUTING_MODE`         | `broadcast`                            | 否                     | 路由模式：`broadcast`/`severity`。                                                                      |
| `INTEGRATION_WEBHOOK_TIMEOUT`      | `10s`                                  | 否                     | 单次 webhook 超时。                                                                                     |
| `INTEGRATION_RETRY_MAX`            | `5`                                    | 否                     | 最大重试次数。                                                                                          |
| `INTEGRATION_RETRY_BASE_DELAY`     | `2s`                                   | 否                     | 退避基线。                                                                                              |
| `INTEGRATION_RETRY_MAX_DELAY`      | `60s`                                  | 否                     | 退避上限。                                                                                              |
| `INTEGRATION_CONSUMER_ACK_WAIT`    | `90s`                                  | 否                     | Consumer ack 等待时间。                                                                                 |
| `INTEGRATION_DLQ_PUBLISH_TIMEOUT`  | `5s`                                   | 否                     | DLQ 发布超时。                                                                                          |

### 3.1.1 integration 通道格式与事件支持

| 通道       | 出站格式                                    | 支持事件类型      | 说明                                           |
| ---------- | ------------------------------------------- | ----------------- | ---------------------------------------------- |
| `webhook`  | 原样转发上游事件 JSON（`application/json`） | `alert`、`weekly` | 不改写业务字段，接收端按治理事件 schema 解析。 |
| `wecom`    | `text` 模板消息                             | `alert`、`weekly` | 发送企业微信机器人文本模板。                   |
| `dingtalk` | `text` 模板消息                             | `alert`、`weekly` | 发送钉钉机器人文本模板。                       |
| `feishu`   | `text` 模板消息                             | `alert`、`weekly` | 发送飞书机器人文本模板。                       |

### 3.2 回调转发链路

| 变量名                                   | 默认值                                  | 必填条件                   | 说明                                                                                              |
| ---------------------------------------- | --------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------- |
| `INTEGRATION_CALLBACK_STREAM`            | `INTEGRATION_CALLBACK_EVENTS`           | 否                         | 回调事件消费 stream。                                                                             |
| `INTEGRATION_CALLBACK_SUBJECT`           | `integration.callback.events`           | 否                         | 回调事件消费 subject。                                                                            |
| `INTEGRATION_CALLBACK_TOPIC`             | 空                                      | 否                         | `INTEGRATION_CALLBACK_SUBJECT` 的兼容别名；二者同时配置时以 `SUBJECT` 为准。                      |
| `INTEGRATION_CALLBACK_DURABLE`           | `INTEGRATION_CALLBACK_EVENT_SINK`       | 否                         | 回调事件 durable consumer。                                                                       |
| `CONTROL_PLANE_BASE_URL`                 | 空                                      | 启动必填                   | control-plane 基础地址。                                                                          |
| `INTEGRATION_CALLBACK_PATH`              | `/api/v1/integrations/callbacks/alerts` | 否                         | 回调转发路径，支持相对路径并自动标准化。                                                          |
| `INTEGRATION_CALLBACK_SECRET`            | 空                                      | 非测试环境启动必填         | integration 写入 `X-Integration-Callback-Secret`，并用于入站 callback 鉴权。                      |
| `INTEGRATION_CALLBACK_SIGNATURE_TTL`     | `5m`                                    | 否                         | callback 签名时间窗；integration 用于配置转发签名窗口，control-plane 用于校验 timestamp/nonce。   |

integration 入站 endpoint：`POST /v1/callbacks/alerts`。

### 3.2.1 callback 签名头约定

| 请求头 | 写入方 | 校验/消费方 | 说明 |
| ------ | ------ | ----------- | ---- |
| `X-Integration-Callback-Secret` | integration | integration 入站 handler、control-plane callback 路由 | 共享密钥鉴权头；control-plane 侧读取 `x-integration-callback-secret`（HTTP 头大小写不敏感）。 |
| `X-Integration-Callback-Source` | integration | control-plane（可选消费） | 回调来源标识，当前值为 `api` 或 `nats`，用于排障与审计。 |
| `X-Integration-Callback-Timestamp` | integration | control-plane callback 路由 | 时间戳头（Unix 秒字符串），参与签名串。 |
| `X-Integration-Callback-Nonce` | integration | control-plane callback 路由 | 随机 nonce，参与签名串并用于窗口内防重放。 |
| `X-Integration-Callback-Signature` | integration | control-plane callback 路由 | 签名头：`HMAC-SHA256(secret, timestamp + "\n" + nonce + "\n" + body)`。 |

### 3.2.2 callback 时间窗配置（当前实现）

| 配置项 | 默认值 | 作用阶段 | 说明 |
| ------ | ------ | -------- | ---- |
| `INTEGRATION_CALLBACK_SIGNATURE_TTL` | `5m` | callback 防重放 | integration/control-plane 共用签名窗口；control-plane 超窗将返回 `401`。 |
| `INTEGRATION_WEBHOOK_TIMEOUT` | `10s` | 回调 HTTP 转发 | integration 向 control-plane 发送单次 callback 请求的超时时间。 |
| `INTEGRATION_RETRY_MAX` | `5` | 回调失败重试 | callback 转发失败后的最大重试次数。 |
| `INTEGRATION_RETRY_BASE_DELAY` | `2s` | 回调失败重试 | callback 重试指数退避基线。 |
| `INTEGRATION_RETRY_MAX_DELAY` | `60s` | 回调失败重试 | callback 重试退避上限。 |
| `INTEGRATION_CONSUMER_ACK_WAIT` | `90s` | callback 消费确认 | JetStream callback consumer 的 ack 等待窗口。 |
| 固定幂等占位时间窗（非环境变量） | `2m` | control-plane callback 幂等 | `callback_id` 处于 processing 且超时后可重新 claim；当前为代码常量。 |

说明：回调防重放由 `signature(timestamp+nonce+body)` 与窗口内 `nonce` 去重共同保障。

## 4. control-plane / puller 联动变量

| 变量名                                   | 默认值                  | 必填条件           | 说明                                                                                              |
| ---------------------------------------- | ----------------------- | ------------------ | ------------------------------------------------------------------------------------------------- |
| `INTEGRATION_CALLBACK_SECRET`            | 空                      | 生产环境必填       | 校验 `POST /api/v1/integrations/callbacks/alerts` 请求头 `x-integration-callback-secret`。      |
| `AUTH_DISABLE_LOCAL_LOGIN`               | `false`                 | 否                 | 设为 `true/1/yes/on` 时，`GET /api/v1/auth/providers` 不返回本地账号（邮箱密码）登录入口。         |
| `AUTH_EXTERNAL_PROVIDERS_JSON`           | 空                      | 否                 | 外部登录提供方 JSON 数组，供 `GET /api/v1/auth/providers` 暴露给前端（支持 `oauth2/oidc/sso`）。 |
| `AUTH_EXTERNAL_ASSERTION_SECRET`         | 空                      | 外部断言登录必填   | `POST /api/v1/auth/external/login` 的 HMAC 签名密钥。                                               |
| `AUTH_EXTERNAL_ASSERTION_TTL_SECONDS`    | `300`                   | 否                 | 外部断言登录时间窗（秒），默认 5 分钟，最大 3600 秒。                                                |
| `PULLER_BASE_URL`                        | `http://127.0.0.1:8086` | 否                 | control-plane 在 `sessions/search` 触发 puller `sync-now` 的基础地址。                            |
| `PULLER_SYNC_TIMEOUT_MS`                 | `1200`                  | 否                 | 单次 puller `sync-now` 请求超时（毫秒），有效范围 `100-30000`，越界会被夹取到边界。               |
| `PULLER_SYNC_RETRY_MAX_ATTEMPTS`         | `3`                     | 否                 | puller `sync-now` 失败后的最大尝试次数（含首轮请求）。                                            |
| `PULLER_SYNC_RETRY_BASE_BACKOFF_MS`      | `200`                   | 否                 | 指数退避基线（毫秒）。                                                                            |
| `PULLER_SYNC_RETRY_MAX_BACKOFF_MS`       | `2000`                  | 否                 | 指数退避上限（毫秒），应大于或等于基线值。                                                        |

说明：当前实现已不使用 `CONTROL_PLANE_CALLBACK_*` 系列变量。

`AUTH_EXTERNAL_PROVIDERS_JSON` 示例：

```json
[
  {
    "id": "github",
    "type": "oauth2",
    "displayName": "GitHub OAuth",
    "authorizationUrl": "https://github.com/login/oauth/authorize",
    "enabled": true
  },
  {
    "id": "corp-oidc",
    "type": "oidc",
    "displayName": "企业 OIDC",
    "issuer": "https://idp.example.com/",
    "authorizationUrl": "https://idp.example.com/auth"
  }
]
```

### 4.1 外部断言登录签名规范

`POST /api/v1/auth/external/login` 的签名串采用换行拼接：

```text
providerId + "\n" + externalUserId + "\n" + email + "\n" + tenantId + "\n" + timestamp + "\n" + nonce
```

1. 签名算法：`HMAC-SHA256(secret, canonical_payload)`，输出 64 位十六进制字符串。
2. `tenantId` 可为空字符串（当请求不指定租户时）。
3. 服务端会校验：
   - 时间窗（`AUTH_EXTERNAL_ASSERTION_TTL_SECONDS`）
   - nonce 防重放（同 provider 下 nonce 在窗口内只能使用一次）

## 4.2 services/puller 后台同步重试变量

| 变量名                        | 默认值 | 必填条件 | 说明                                                              |
| ----------------------------- | ------ | -------- | ----------------------------------------------------------------- |
| `PULLER_JOB_MAX_RETRIES`      | `3`    | 否       | puller 后台 `sync_jobs` 失败时的最大重试次数（不含首轮执行）。    |
| `PULLER_JOB_RETRY_BASE_DELAY` | `5s`   | 否       | puller 后台失败重试指数退避基线，按 `base * 2^(attempt-1)` 计算。 |

说明：本节变量由 `services/puller` 使用，和 `sessions/search` 的 `PULLER_SYNC_RETRY_*`（control-plane 侧触发）是两条独立链路。

## 5. DATABASE_URL 依赖说明

1. `services/integration`：当前告警分发与回调转发链路不强依赖 `DATABASE_URL`。
2. `services/ingestion-gateway`：当前不强依赖 `DATABASE_URL`。
3. 强依赖 `DATABASE_URL` 的服务：`services/normalizer`、`services/analytics`、`services/governance`、`services/puller`、`services/archiver`。
4. `apps/control-plane`：未配置 `DATABASE_URL` 时会回退内存模式；生产环境建议显式配置 PostgreSQL。

## 6. 回调链路一致性对照表

| 对照项                      | integration 侧                                                                                           | control-plane 侧                             | 校验脚本                                                                              |
| --------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------- |
| Stream/Subject/Durable 绑定 | `INTEGRATION_CALLBACK_STREAM` / `INTEGRATION_CALLBACK_SUBJECT`(`TOPIC`) / `INTEGRATION_CALLBACK_DURABLE` | -                                            | `scripts/check-callback-stream-binding.sh`                                            |
| 转发目标 URL                | `CONTROL_PLANE_BASE_URL` + `INTEGRATION_CALLBACK_PATH`                                                   | 路由 `/api/v1/integrations/callbacks/alerts` | `scripts/test-callback-chain-targeted.sh`                                             |
| 回调鉴权 Secret             | header `X-Integration-Callback-Secret`                                                                   | 校验 `x-integration-callback-secret`         | `scripts/test-callback-chain-targeted.sh`、`scripts/e2e-governance-callback-chain.sh` |
| 回调来源标识                | header `X-Integration-Callback-Source`                                                                   | 用于日志与审计定位来源（`api`/`nats`）       | `scripts/test-callback-chain-targeted.sh`、`scripts/e2e-governance-callback-chain.sh` |
| 回调签名与防重放            | header `Timestamp/Nonce/Signature`，`INTEGRATION_CALLBACK_SIGNATURE_TTL`                                 | 校验签名、时间窗、nonce 去重                 | `scripts/test-callback-chain-targeted.sh`、`scripts/e2e-governance-callback-chain.sh` |
| 回调时间窗                  | `INTEGRATION_WEBHOOK_TIMEOUT` / `INTEGRATION_RETRY_*` / `INTEGRATION_CONSUMER_ACK_WAIT`                 | 幂等占位固定窗口 `2m`（代码常量）            | `scripts/test-callback-chain-targeted.sh`、`scripts/e2e-governance-callback-chain.sh` |

### 6.1 回调运维检查项

1. 发布前执行 `bun run check:callback-stream-binding`，确认 stream/subject/durable 绑定无冲突。
2. 核对 integration 与 control-plane 的 `INTEGRATION_CALLBACK_SECRET` 一致，避免出现持续 `401`。
3. 校验 `INTEGRATION_CALLBACK_SIGNATURE_TTL` 两端一致，并确认 `Timestamp/Nonce/Signature` 头可达。
4. 校验时间窗组合：`INTEGRATION_RETRY_MAX_DELAY >= INTEGRATION_RETRY_BASE_DELAY`，且建议 `INTEGRATION_CONSUMER_ACK_WAIT > INTEGRATION_WEBHOOK_TIMEOUT`。
5. 执行 `bun run test:callback-chain-targeted` 与 `bun run test:e2e-governance-callback-chain`，确认回调链路可达且可回归。
6. 观察 integration 日志中的 callback `retry/dlq` 与 control-plane `401/4xx/5xx`，出现持续异常时优先排查 secret、签名与目标 URL。

## 7. FR-505 三平台最小冒烟脚本变量

以下变量仅用于 `scripts/smoke-fr505-cross-platform.sh`，不属于生产运行配置：

| 变量名                     | 默认值         | 必填条件 | 说明                                                   |
| -------------------------- | -------------- | -------- | ------------------------------------------------------ |
| `FR505_CONTROL_PLANE_PORT` | `随机可用端口` | 否       | 冒烟时 control-plane 监听端口；未设置时自动分配。      |
| `FR505_MOCK_INGEST_PORT`   | `随机可用端口` | 否       | 冒烟时本地 mock ingestion 监听端口；未设置时自动分配。 |
| `FR505_SMOKE_TIMEOUT_MS`   | `20000`        | 否       | `/api/v1/health` 与 mock 健康探测总超时（毫秒）。      |
| `FR505_SMOKE_INTERVAL_MS`  | `250`          | 否       | 健康探测轮询间隔（毫秒）。                             |

建议联调命令：

```bash
bun run check:callback-stream-binding
bun run test:callback-chain-targeted
bun run test:e2e-governance-callback-chain
bun run smoke:fr505
```

## 8. 支持矩阵门禁说明（无新增环境变量）

`scripts/check-support-matrix.ts` 已升级为 P0/P1 全覆盖、双向严格一致校验，并会检查 parser 入口（`featureConnector.Parse` 与 `parseWithConnector`）。该脚本不读取任何环境变量，无需新增配置项。
