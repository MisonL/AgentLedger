# AgentLedger 全量规划功能核对矩阵

- 文档版本：v1.4
- 更新时间：2026-03-10
- 目标：把历史规划、当前实现、验证证据与结论状态统一到一份可持续维护的功能矩阵。

## 1. 判定规则

本矩阵统一使用以下结论：

- `已实现且已验证`
- `已实现但缺明确验证入口`
- `部分实现`
- `仅规划/长期蓝图`

判定口径：

1. `docs/01` 定义 V1 基础账本能力。
2. `docs/11` 定义老板确认的企业增强路线。
3. `docs/15` 与 `README` 定义当前主干事实清单。
4. `docs/10` 中超出当前事实清单、且无代码证据的内容，统一记为 `仅规划/长期蓝图`。

## 2. 基础账本能力

| 能力项 | 规划来源 | 当前实现入口 | 现有证据 | 回归验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 多源接入模型与 Source 生命周期 | `docs/00`、`docs/01`、`docs/15` | `apps/control-plane/src/routes/sources.ts` | Source create/list/delete、tenant 隔离、health/parse-failures API 已在 control-plane 全量测试覆盖 | `bun run test` | 已实现且已验证 |
| SSH 实时读取 + `realtime/sync/hybrid` + 同步任务编排 | `docs/00`、`docs/01`、`docs/10`、`docs/15` | `services/puller`、`apps/control-plane/src/routes/sessions.ts` | puller 侧有 SSH/source/sync 逻辑与测试；`sessions/search` 已支持同步失败重试与 freshness 返回 | `bun run test`、`go test ./services/puller` | 已实现且已验证 |
| 来源健康、解析失败追踪、同步失败重试 | `docs/01`、`docs/15` | `apps/control-plane/src/routes/sources.ts`、`services/puller` | `/sources/:id/health`、`/parse-failures`、`sync_jobs` 相关逻辑与控制面测试存在 | `bun run test` | 已实现且已验证 |
| Agent 自动采集与 P0/P1 客户端矩阵 | `docs/01`、`docs/09`、`README`、`docs/15` | `clients/agent/collect.go`、`scripts/check-support-matrix.ts` | `agent collect`、默认目录、工具别名、P0/P1 对齐检查、Puller P0 解析准确率门禁都已存在 | `bun run check:support-matrix`、`bun run check:puller-p0-accuracy`、`bun run smoke:fr505` | 已实现且已验证 |
| 会话检索、详情、事件时间线、导出 | `docs/01`、`docs/15` | `apps/control-plane/src/routes/sessions.ts`、`apps/control-plane/src/routes/exports.ts` | `/sessions/search`、`/sessions/:id`、`/events`、sessions export 都在 control-plane 全量测试中覆盖 | `bun run test` | 已实现且已验证 |
| usage 聚合、热力图、日期下钻、weekly summary | `docs/01`、`docs/15`、`README` | `apps/control-plane/src/routes/usage.ts`、`services/analytics` | daily/monthly/models/sessions、heatmap、drilldown、weekly-summary 代理与 analytics 测试均存在 | `bun run test`、`go test ./services/analytics` | 已实现且已验证 |
| 定价目录与成本口径 | `docs/01`、`docs/15` | `apps/control-plane/src/routes/pricing.ts`、repository pricing 逻辑 | pricing catalog 读写、usage 口径“日志优先/估算补齐”已在 control-plane 测试覆盖 | `bun run test` | 已实现且已验证 |
| 预算策略、阈值分级、解冻审批与预算绑定校验 | `docs/01`、`docs/10`、`docs/15` | `apps/control-plane/src/routes/budgets.ts`、`services/governance/main.go` | budgets、release-requests、callback budget binding 在 control-plane 与 governance 测试中有证据 | `bun run test`、`go test ./services/governance` | 已实现且已验证 |

## 3. 企业治理能力

| 能力项 | 规划来源 | 当前实现入口 | 现有证据 | 回归验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 告警列表与状态流转 | `docs/10`、`docs/15` | `apps/control-plane/src/routes/alerts.ts` | alerts list、ack、resolve 已在 control-plane 与 web-console 测试中覆盖 | `bun run test` | 已实现且已验证 |
| 告警编排规则、模拟与执行日志 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/alerts.ts`、`services/governance/main.go` | orchestration rules CRUD、simulate、executions 过滤与治理页工作台均有测试 | `bun run test`、`go test ./services/governance` | 已实现且已验证 |
| `alert/weekly -> governance -> integration` 分发闭环 | `docs/11`、`docs/15`、`README` | `services/governance`、`services/integration` | `dispatchMode/dedupeHit/suppressed/conflictRuleIds` 已进入 execution log；integration 优先消费 orchestration channels | `bun run test:e2e-governance-routing`、`bun run test:e2e-integration-routing` | 已实现且已验证 |
| 告警外部联动运维与失败治理深化 | `docs/11`、`docs/15`、`docs/18` | `apps/control-plane/src/routes/alerts.ts`、`apps/control-plane/src/routes/integration-dlq.ts`、`apps/web-console/src/App.tsx` | 已补 external link 运维视图、失败聚合查询、单条/按告警批量重试、Integration DLQ recovery job、失败审计报表，以及长期趋势/容量运维视图；control-plane 与 web-console 定向回归已补齐 | `bun test apps/control-plane/test/api.test.ts -t \"integration alert failure\"`、`bun run --cwd apps/web-console test -- test/api.test.ts -t \"fetchIntegrationAlertFailureTrends\"`、`bun run --cwd apps/web-console test -- test/app.test.tsx -t \"治理页支持加载长期趋势/容量运维视图\"` | 已实现且已验证 |
| 数据主权策略、地域映射、复制任务 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/residency.ts`、`apps/control-plane/src/routes/api-v2.ts` | v1/v2 policy、region mappings、replication create/approve/cancel 与控制台工作台均有测试 | `bun run test` | 已实现且已验证 |
| Rule Hub 首版闭环 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/rules.ts`、`apps/web-console/src/App.tsx` | 资产、版本、发布、回滚、审批、审计、`scopeBinding` 录入展示均有前后端回归 | `bun run test` | 已实现且已验证 |
| Rule Hub 双人审批、版本 diff | `docs/11`、`docs/15` | `apps/control-plane/src/routes/rules.ts`、`apps/web-console/src/App.tsx` | 资产支持 `requiredApprovals=1/2`，publish 前校验审批人数；新增 `versions/diff` API 与控制台 diff 视图；前后端回归已补齐 | `bun run --cwd apps/control-plane test test/api.test.ts`、`bun run --cwd apps/web-console test` | 已实现且已验证 |
| MCP 工具治理首版闭环 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/mcp.ts` | policy / approvals / evaluate / invocations 与租户隔离均有全量测试证据 | `bun run test` | 已实现且已验证 |
| MCP 更复杂审批编排 | `docs/10`、`docs/11` | `apps/control-plane/src/routes/mcp.ts`、`apps/control-plane/src/data/repository.ts`、`apps/web-console/src/App.tsx` | 当前已补 `single_stage/two_stage/multi_stage`、`approvalStages[]`、`approvalWorkflow` 分支跳转、`evaluationTimestamp` 驱动的 `timeWindow` 条件匹配、workflow 持久化恢复，以及治理页多级节点/转移配置与结果展示；更重型企业编排器与图形化设计器仍未落地 | `bun run --cwd apps/control-plane test test/api.test.ts -t "mcp 路由：支持"`、`bun run --cwd apps/web-console test -- test/app.test.tsx -t "MCP|approvalStages|multi_stage"` | 已实现，待继续深化 |

## 4. 身份、审计与合规

| 能力项 | 规划来源 | 当前实现入口 | 现有证据 | 回归验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- |
| tenant / org / member / device / agent / source-binding | `docs/10`、`docs/15` | `apps/control-plane/src/routes/identity.ts` | identity 全量 API 测试覆盖 tenant/org/member/device/agent/source-binding 正常流与租户隔离 | `bun run test` | 已实现且已验证 |
| provider discovery、OAuth/OIDC code exchange、外部断言登录 | `docs/10`、`docs/15` | `apps/control-plane/src/routes/auth.ts` | `auth/providers`、`external/exchange`、`external/login` 已有全量测试 | `bun run test` | 已实现且已验证 |
| 审计查询、导出、合规取证包 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/audits.ts`、`src/security/evidence-bundle.ts` | `/audits`、`/audits/export`、`/audits/evidence-bundle`、本地 `evidence:verify` 均有测试与脚本 | `bun run test`、`bun run evidence:verify -- --file <bundle.json>` | 已实现且已验证 |
| Agent 生命周期审计 | `docs/10` | `apps/control-plane/src/routes/identity.ts`、`clients/agent/main.go` | 已补 `/api/v1/agents/lifecycle-events` create/list 与 `doctor/status/update check` 可选生命周期上报；control-plane / agent 定向测试已覆盖 | `bun run test`、`go test ./clients/agent` | 已实现且已验证 |
| SCIM / SAML / MFA / 风险登录 | `docs/10` | `apps/control-plane/src/routes/auth.ts`、`apps/control-plane/src/routes/identity.ts` | 当前已有 OAuth/OIDC/provider discovery/assertion login，并已补 `saml` provider discovery、provider `requireMfa`、本地 MFA 静态门槛、基于风险头的实际阻断，以及租户级 SCIM users/groups 最小同步入口；更深企业 IAM 集成仍未完成 | `bun run test` | 部分实现 |
| DLP / Legal Hold / KMS 管理密钥 | `docs/10`、`docs/11` | `apps/control-plane/src/routes/audits.ts` | 当前已补 Legal Hold create/list/release、对 `audit/audit_export/evidence_bundle` 的保护与审计联动，并新增 audit export/evidence bundle 的 DLP `off/redact/block` 最小治理；更完整敏感信息策略与 KMS 管理密钥仍未完成 | `bun run test` | 部分实现 |

## 5. 开放平台、质量、回放与集成

| 能力项 | 规划来源 | 当前实现入口 | 现有证据 | 回归验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- |
| OpenAPI、API Key、Webhook、Webhook replay | `docs/11`、`docs/15` | `apps/control-plane/src/routes/open-platform.ts` | `/openapi.json`、API Key 生命周期、Webhook CRUD、replay tasks 全量测试覆盖 | `bun run test` | 已实现且已验证 |
| `replay.job.*` 兼容 + `replay.run.*` 正式事件 | `docs/releases/2026-03-06-governance-routing-and-replay-run-events.md`、`docs/15` | `packages/contracts/src/types.ts`、`apps/control-plane/src/routes/open-platform.ts` | contracts、OpenAPI、Webhook replay、README/release note 已统一到 `replay.run.*` 优先口径 | `bun run test`、`bun run sdk:check` | 已实现且已验证 |
| quality v2 基础版 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/quality.ts`、`api-v2.ts`、`apps/web-console/src/App.tsx` | 评估写入、日报、相关性、项目趋势、评分卡与控制台工作台均有测试 | `bun run test`、`bun run --cwd apps/web-console test` | 已实现且已验证 |
| quality 高级版预测与建议 | `docs/11` | `apps/control-plane/src/routes/api-v2.ts`、`apps/control-plane/src/routes/quality-advice-execution.ts`、`apps/control-plane/src/data/repository.ts`、`apps/web-console/src/App.tsx` | 当前已补 `forecast/advice`、`advice executions` execute/list/cancel、自动治理命中的 advice execution 持久化、`evaluationScoreThreshold/triggerOnEvaluationFailure/triggerOnReplayRegression/defaultActionType`、`trendDirection/projectedDelta/basisWindowCount/rationale/riskDrivers/recommendedActions`、`explanation/confidence/why/automationReadiness/executionHint/executionOptions`、`mcp approval -> advice execution` 审批后继续执行/驳回收口链路，以及治理页 explain/执行工作台；更高级预测模型与更细粒度自动执行策略矩阵仍待继续深化。补充（2026-03-10）：`strategyMatrix` 已补 `minSampleCount/minPassRate` 的校验、匹配门槛与 simulate 回显（control-plane/web-console 均有测试覆盖）。 | `bun run --cwd apps/control-plane test test/api.test.ts`、`bun run --cwd apps/web-console test -- test/api.test.ts test/app.test.tsx` | 已实现，待继续深化 |
| replay v2 基础回放能力 | `docs/11`、`docs/15` | `apps/control-plane/src/routes/api-v2.ts`、`apps/control-plane/src/routes/replay.ts`、`apps/web-console/src/App.tsx` | datasets / runs / diffs / artifacts / materialize、控制台首版工作台、SDK 生成链均存在 | `bun run test`、`bun run sdk:check` | 已实现且已验证 |
| “回放实验室”完全体 | `docs/11` | `apps/control-plane/src/routes/api-v2.ts`、`apps/web-console/src/App.tsx` | 当前已有基础回放/差异/工件，并补了 experiments 的 `create/list/detail/run/cancel/results/compare/workflow/artifacts`、批量 `experiments/compare`、`sourceAdviceId/runStatusSummary/aggregateSummary` 与 repository 持久化；治理页已可直接查看单实验对比、批量实验对比与工作流节点视图；更完整的实验编排与基线管理扩展仍待继续深化。补充（2026-03-10）：experiment run 已支持 `candidateLabels/skipIfRunning` 入参（`skipIfRunning` 默认 `true`），治理页新增“忽略运行中”按钮用于强制启动（`skipIfRunning=false`）。 | `bun run test` | 已实现，待继续深化 |
| Integration 外部通道分发与 callback 闭环 | `docs/15`、`README` | `services/integration`、`apps/control-plane/src/routes/integration-callbacks.ts` | `webhook/wecom/dingtalk/feishu/email/email_webhook/ticket`、callback 签名/幂等、防重放均有真实回归 | `bun run test:callback-chain-targeted`、`bun run test:e2e-integration-routing`、`bun run test:e2e-governance-callback-chain` | 已实现且已验证 |
| `governance -> integration -> downstream` 跨服务 smoke | `docs/05`、`docs/15` | `services/governance/governance_integration_smoke_test.go` | 真实 PG + 嵌入式 NATS + 子进程 `integration/control-plane` 的 smoke 已存在；脚本入口已扩到 `alert/weekly` 下游分发 + `external_status_sync` 回写闭环，并已在真实 PG 环境实跑验证（详见 `docs/17`） | `bun run test:e2e-governance-integration-downstream` | 已实现且已验证 |

## 6. Agent / C/S / 三平台 / 归档 / 发布门禁

| 能力项 | 规划来源 | 当前实现入口 | 现有证据 | 回归验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- |
| Agent Push（HTTP/gRPC）、OIDC、doctor | `docs/10`、`README` | `clients/agent/main.go`、`collect.go`、`oidc.go`、`run_grpc.go` | agent 支持 `collect / oidc / doctor / run`，gRPC TLS/mTLS 参数与 doctor 检查有单测 | `bun run test`、`bun run smoke:fr505` | 已实现且已验证 |
| gRPC mTLS / TLS 端到端能力 | `docs/10` | `clients/agent/run_grpc.go`、`services/ingestion-gateway/grpc_server.go`、`clients/agent/run_grpc_e2e_test.go` | 客户端与 ingestion-gateway 有 TLS/mTLS 单测，且新增 agent 侧本地 gRPC mTLS 端到端测试 | `go test ./clients/agent -run TestSendIngestRequestGRPC_MTLSEndToEnd -count=1` | 已实现且已验证 |
| Store-and-Forward、本地队列、Policy Agent、远程配置下发、Updater、静默安装 | `docs/10` | `clients/agent/main.go`、`apps/control-plane/src/routes/system-config.ts`、`docs/templates/*` | 当前已补 `agent run --queue-dir/AGENT_QUEUE_DIR` 本地持久队列与 `agent status` 队列观测、`config pull/activate/rollback/watch`、配置包 `publish/watch/latest + 最小审批门槛`、`update check/download/apply/rollback/status`、下载阶段 `checksum + 可选 Ed25519 签名` 校验、服务端 rollout ring/percentage/minVersion 筛选、三平台静默安装模板；真正的平台侧配置中心、复杂灰度编排、后台常驻升级守护与企业分发链路仍未完成 | `go test ./clients/agent`、`bun run smoke:fr505` | 部分实现 |
| 归档存储 `local/object/hybrid` + 本地 ZSTD 压缩 | `docs/10`、`README`、`docs/13` | `services/archiver/main.go` | `ARCHIVE_MODE`、`ARCHIVE_LOCAL_COMPRESSION=zstd`、对象/本地双写测试已存在 | `go test ./services/archiver` | 已实现且已验证 |
| FR-505 三平台最小冒烟 | `docs/01`、`docs/05`、`README` | `scripts/smoke-fr505-cross-platform.sh` | 当前 CI 与本地都已可运行最小三平台 smoke | `bun run smoke:fr505` | 已实现且已验证 |
| Agent 多架构构建与产物校验 | `docs/05`、`README` | `scripts/build-agent-cross.sh`、`scripts/verify-agent-cross-artifacts.sh` | linux/darwin/windows 多架构产物构建与校验脚本、CI job 均存在 | `bun run build:agent-cross`、`bun run verify:agent-cross` | 已实现且已验证 |
| CI / Release 门禁链 | `docs/05`、`README` | `.github/workflows/ci.yml`、`.github/workflows/release.yml` | baseline 三平台、coverage、callback、integration、governance、agent-cross，以及本轮补入的 `agent-distribution` 组装/验收都已接入；历史远端 run `22788548732` 已成功，当前工作树以 `docs/17` 本地回归记录为准 | `.github/workflows/ci.yml`、`.github/workflows/release.yml`、`docs/17-回归验证执行记录.md` | 已实现且已验证 |
| 多环境 / 多集群 / SaaS 多租户部署形态 | `docs/10` | `docs/19-部署形态基线与预检.md`、`scripts/check-deployment-baseline.ts` | 当前已补三类部署基线文档与预检脚本；完整多环境/多集群/SaaS 控制面 IaC 与运维资产仍未完成 | `bun run ./scripts/check-deployment-baseline.ts --profile governance --env-file ./.env && bun run ./scripts/check-deployment-baseline.ts --profile integration --env-file ./.env` | 部分实现 |
